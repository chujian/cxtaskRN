'use strict'

/**
*评论action
*/


let fetchCommentList = (task_id,token)=>{
  return dispatch => {
    //return fetch()
  }
}

let requestTaskInfo = () => {
  return{
    type: TYPES.TASK_REQUEST
  }
}
